# Fable audit — predictions before compute (2026-09-06). Separate file; Codex files untouched.

Read: CERTIFICATE.md, RESULTS-parity-tail.md, PURE-TAIL-LEMMA.md, lee_yang_bridge/RESULTS.md, certify.py, parity_tail.py, pure_tail_bound.py, test_certify.py; arXiv:2608.24827v2 (Zhu, 2 Sep 2026) abstract and HTML.

## Already done (this session, before this file)
- Hand re-derivation of PURE-TAIL-LEMMA.md: Fourier form of Q, a(t)=Re psi(1/4+it/2)-log pi and its monotonicity, a(0) closed form, leakage bound 2 omega_j/(sqrt(L)(omega_j^2-R^2)) [in fact an equality up to phase], eta, mass B=2.94197, pole-term parity bounds, constants 0.5600/0.4428. No defect found. MEASURED (by hand).
- Normalization of the form in PREDICTIONS.md against CCM 2025 eqs (3.7)-(3.11) and our round-5 implementation: identical (W_{0,2} = 2(int f e^{x/2})(int f e^{-x/2}); archimedean and prime terms match). MEASURED.
- Replay of test_certify.py and test_parity_tail.py: 5/5 pass on the saved balls. Preregistration commits precede result commits (d7430f3 06:27 -> ae7424b 07:05 -> 535af86 07:08 on 09-05; 85c20ab 08:02 -> 80fc870 08:04 on 09-06). MEASURED.
- Third implementation: our CCM Fourier-basis code (ccm_triples.py) at lambda^2 = e^{1.4} (window [-0.7,0.7], prime powers 2,3,4), N=60: smallest eigenvalue +4.467e-13 (even), second +2.808e-10. Codex sine basis N=32: even 5.972e-13, odd 3.313e-10. Same magnitudes, same ordering, smaller in the larger space. MEASURED (numerical, not certified).
- Zhu v2: positivity certificate on [-0.8,0.8] for ALL complex L^2 functions (Thm 1.2 / Cor 6.3), bound 8.9e-18, computer-assisted; zeros enter only proposal/validation, not the lower bound (his Sec 1.5). Certificate data: N=200 even Legendre modes, 50 dp, 800 panels x 32 GL, tails < 1e-100; no URL for the archived matrix found in the HTML. UNVERIFIED (not reproduced yet).

## D1 — why mode-index block splitting cannot close Codex's gap (diagnostic, not certificate)
Build the sine-basis matrix to N=48 (Codex's certify functions, 160 bits, tolerance 1e-20, eps 1e-25). A = modes 1..32, D = modes 33..48, C = coupling. Per parity sector: lambda_min(A), lambda_min(D), ||C||_F, and the near-null direction xi_A of A with its coupling energy k := (C^T xi_A)^T D^{-1} (C^T xi_A).
PREDICTED: the naive Schur condition ||C||^2 < lambda_min(A) lambda_min(D) FAILS by many orders (||C|| ~ 1e-2 vs sqrt(6e-13 * O(1)) ~ 1e-6), while the full 48-mode matrix is still positive (Codex's trend). Hence positivity survives only because the near-null direction is nearly orthogonal to the coupling: PREDICTED k < lambda_min(A) with ratio k/lambda_min(A) < 0.5. Reading if it holds: the whole difficulty sits in one direction (the CCM ground state), and any mode-index Schur route must control that direction exactly, which no norm bound does. Kill: k > lambda_min(A) with the 48-block still positive is impossible; k > lambda_min(A) with the 48-block negative would contradict Codex's positivity trend and our cross-check.

## D2 — independent reduced reproduction of Zhu's frequency-envelope reduction at OUR window L=0.7
Q(f) = P(f) + int Psi_L(t)|F(t)|^2 dt, Psi_L(t) = a(t) - sum_w cos(u t). For |t| >= T#, Psi_L >= beta* := a(T#) - B (a increasing, cos <= 1). Then Q >= R := P + int_{|t|<=T#} (Psi_L - beta*)|F|^2 dt + beta* ||f||^2, an identity-plus-compact form on L^2[-L,L]. Expand even f in Legendre polynomials P_{2m}(x/L); F-hat of P_n is 2 L (-i)^n j_n(L t) (spherical Bessel), exact. Build the N_even x N_even matrix at T# in {120,150} with 30-40 dp quadrature; Cholesky; report lambda_min(R) and the Legendre-tail size of the minimizing vector.
PREDICTED: beta* > 0 at T#=150 (a(150) ~ log 75 - log pi ~ 3.17 > B = 2.94; margin small, ~0.23). PREDICTED: R is positive definite for even functions with lambda_min(R) between 1e-18 and 1e-12 (it must be <= 4.5e-13, the true minimum; Zhu got 8.9e-18 at L=0.8). PREDICTED: the minimizing vector's Legendre coefficients decay super-exponentially so that N_even=100 suffices (tail < 1e-30). Kill: lambda_min(R) < 0 at both T#, meaning the envelope loses too much at this window and Zhu's method would need his sharper machinery here; that would be MEASURED and would leave Codex's gap open by this route.
Status if it works: a numerical (not interval-certified) reproduction of the reduction that covers ALL even f on [-0.7,0.7], closing Codex's "modes 33..4096 + coupling" gap at this window. Not RH; not new (Zhu 2026).

## D3 — Lee–Yang, analytically (no compute)
To be written in FABLE-AUDIT.md: the hypothesis the circle theorem needs (ferromagnetic = all pair couplings >= 0, positive weights), why Newman's 1976 framing already places the zeta kernel Phi at the edge of the Laguerre–Polya class (Lambda >= 0 by Rodgers–Tao), and the termwise obstruction: Phi(u) = sum (2 pi^2 n^4 e^{9u} - 3 pi n^2 e^{5u}) e^{-pi n^2 e^{4u}} has negative terms, so it is not a ferromagnetic weight as written.
