# Parity and tail audit — frozen before new computation

2026-09-05. L=7/10 unchanged, no zero ordinates. Separate spatially even
sines (odd index j) and spatially odd sines (even index j). Principal
restrictions N=16,24,32, meaning N/2 modes per reflection sector.

PREDICTED: both authentic sectors remain positive; their finite minima
decrease as N grows; the even sector has the smaller minimum. Controls
archimedean-only, +/-10% prime-log shifts run before authentic diagnostics
and retain a negative direction in at least one sector. Mirror symmetry
is exact for every model, not a discriminator. Wrong predictions stay.

Attempt entry enclosures with Arb, 512-bit arithmetic, epsilon=10^-65,
absolute/relative integral targets 10^-55. Carry the explicit cutoff bound.
For authentic sectors and each N test fixed shifts 10^-k I for
k=8,12,16,20,24,28,32,36,40 using interval LDL; report the strongest
successful bound, or UNVERIFIED. Also report midpoint eigenvalues as
MEASURED approximations, not enclosures. Compare the N16 block against
the previous independent integration output.

Attempt an analytic infinite-tail estimate separately. PREDICTED: symmetry
alone does not yield a usable bound. A finite hidden-block calculation is
not an infinite-tail bound. All-domains positivity, RH and novelty remain
UNVERIFIED unless actually proved. No extrapolated decay fit is a theorem.

## Analytic-tail follow-up, frozen before its reported numerical evaluation

The independent audit derived a Fourier leakage estimate for sine indices
j>N. Evaluate it at the deliberately coarse fixed values N=4096, R=256,
L=7/10 using ball arithmetic. PREDICTED: even-sector pure-tail bound >0.5,
odd-sector pure-tail bound >0.3. Controls: same bound for the three altered
models, expected also positive (a pure high-tail bound is not an arithmetic
discriminator). Mutation N=8192, same R; bounds predicted to improve.
This does not compute a 4096-mode matrix or bound its low-tail coupling.
The derivation and these simple cutoff choices use no zero data. An agent's
exploratory N=32,R=10 evaluation was inconclusive before this follow-up was
frozen; it is exploratory, not a preregistered result.
