# D17 — prime powers and the critical exponent (predictions before compute, 2026-09-13)
Two labeled mutations of the compact-window form (floating, D15/D16 machinery, noise floor 3e-14).
Test A (powers only): remove ALL k ≥ 2 terms (4, 8, 9, …) at once, keeping primes, versus removing all k = 1 terms keeping powers. Forms: ζ at L = 0.7 (powers visible: 4); χ₋₄ at L = 1.2 (9); χ₋₃ at L = 1.1 (4, 8); χ₅ at L = 1.2 (4, 8, 9). PREDICTED: removing the powers alone makes at least one parity negative in every form (they are at the √x scale, not a correction); removing the primes alone is far worse (λmin below −0.3 in some parity).
Test B (exponent): replace the weight n^{−1/2} by n^{−σ} for σ ∈ {0.45, 0.48, 0.49, 0.50, 0.51, 0.52, 0.55}, all other terms unchanged (a labeled mutation: the object is no longer any L-function's explicit formula). Forms: ζ at L = 0.6 (margin ≈ 1e-9 even) and χ₋₄ at L = 1.0 (≈ 5e-7). PREDICTED: σ = 0.50 is the unique positive point on the grid for the even sector in both forms; both directions fail already at ±0.01; the failure is asymmetric — σ < 0.5 (heavier primes) fails harder than σ > 0.5.
Kill: if any σ ≠ 0.5 on the grid stays positive in both parities, the "critical exponent is rigid" reading is VOID for that form.
