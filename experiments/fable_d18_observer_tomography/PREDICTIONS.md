# D18 — the observer as the interference pattern of all rays: what tomography lets it know (predictions before compute, 2026-09-13)
Object: the lab's 27-cell ternary cube (cells in GF(3)); the observer sees only line sums along chosen directions. Known (this session): 3 axis directions give 27 sums of rank 19, ghost space dimension 8; the 3⁴ cube with 4 axis directions gives 108 sums of rank 65, ghosts 16.
T1 directions: add lattice direction families to the 3×3×3 cube one at a time — 3 axes, then 6 face diagonals, then 4 body diagonals (13 total). Report rank and ghost dimension over GF(3) after each. PREDICTED: axes 19 (known); axes + face diagonals: ghost dimension ≤ 2; all 13 directions: ghost dimension 0 (full reconstruction, the observer knows the cube).
T2 forgery weight: minimum number of cells that must change together to be invisible to the 3-axis observer (minimum Hamming weight of the ghost space; enumerate all 3⁸ = 6561 ghosts). PREDICTED: 8 (a 2×2×2 alternating-sign block). With face diagonals added: PREDICTED ≥ 12 or the ghost space is empty.
T3 one observer vs four: four separate 27-cubes with 3-axis observers have ghost dimension 4 × 8 = 32 of 108 cells (29.6 %); one 3⁴ cube with 4 axes has 16 of 81 (19.8 %). PREDICTED (analytic, to be confirmed): the single higher-dimensional observer sees a larger fraction; adding the 4D diagonal families reduces the 3⁴ ghosts to 0 with at most 40 directions.
T4 honest change: a single-cell change moves exactly 3 axis sums (one per axis) in the 3-axis code (lab states 4 outputs including the retained source trit). PREDICTED: with all 13 directions it moves exactly 13 sums; two-cell changes move between 13 and 26.
Controls: over the rationals instead of GF(3) the ranks may differ (PREDICTED: 3-axis real rank also 19; the integer ghost space is the classical switching-component space). Kill: if T1 with all 13 directions still has ghosts, the "center observer can know the cube" reading is VOID and consistency stays non-authenticating even with every lattice angle.
