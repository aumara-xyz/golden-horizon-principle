# D19 — does the critical exponent emerge from the window? (predictions before compute, 2026-09-13)
For the labeled mutation n^{−σ} of D17, define per parity the allowed interval I_par(L) = {σ : λmin > 3e-14} near 0.5, found by bisection on each side; the joint interval is I_even ∩ I_odd. Forms: ζ at L ∈ {0.30, 0.35, 0.40, 0.45, 0.50, 0.55, 0.60}; χ₋₄ at L ∈ {0.5, 0.7, 0.9, 1.0}.
PREDICTED: (1) both parity intervals contain 0.5 at every L (positivity at 0.5 is known); (2) the joint interval's width shrinks monotonically with L, from > 0.05 at L = 0.30 (ζ) to < 1e-6 at L = 0.60; (3) the joint interval is NOT centered on 0.5 at small L — its midpoint deviates by more than 10 % of its width at L = 0.30 — and the midpoint converges to 0.5 as the width shrinks (the line "emerges" from the window); (4) for χ₋₄ the two parity intervals lie on opposite sides of 0.5 (even prefers σ < 0.5, odd σ > 0.5, D17) and their intersection is a shrinking neighborhood of 0.5.
Kill: if a joint interval at some L excludes 0.5, the code is wrong (positivity at 0.5 is measured); VOID and debug.
