# D22 — Astra's Test 2: a profile independent of cutoff and basis (Fable, predictions before compute, 2026-09-13)
Executes experiments/astra_d20_review/PREDICTIONS.md §Test 2 (commit 07913fa) after D21/D21.1 (cd147a7). ζ only; L ∈ {0.40, 0.50, 0.60, 0.70}; both parities. Certified R_{L,T} operator lower bounds (all functions: finite block + discarded-space Schur, my D4/D5 machinery parameterized in L, T, NE; 192-bit balls) at T = 160 with 160 modes per parity for all four L; T = 240 and 192 modes for L = 0.7 (cap-limited; others only if time remains). Frozen candidates: the certificate's own frozen eigenbasis minimizer (40-digit decimals, taken from the certified matrix, no reselection), scored exactly with the D9 scorer copy at compact cutoffs 128, then 256, then 512, stopping when the tail bound is < 10 % of the certified lower bound or the cap is hit. A point enters the profile only if lower > 0 and (upper − lower)/lower ≤ 0.10.
Astra's predictions (theirs): the decrease survives qualitatively; at least one existing reduced minimum differs from a resolved full-W bracket by > 10 %; the slope stays a local fit.
Fable's predictions (mine): (1) certified lower bounds (even): L = 0.40 ≈ 1.6e-4, 0.50 ≈ 7e-7, 0.60 ≈ 8e-10, 0.70 ≈ 2.5e-13 at T = 160 (matching the floating D12/D19 numbers to 20 %). (2) Brackets: close at L = 0.40 and 0.50 in both parities (tail bounds ~1e-8 at cutoff 128 are far below those margins); at L = 0.60 the even bracket needs cutoff ≥ 256 and may still fail 10 %; at L = 0.70 the even bracket FAILS to close within the cap (tail ≥ 1e-8 ≫ 2.5e-13) — Astra's "honest result" case; the odd bracket at 0.70 also fails (margin 1.6e-10 vs tail ~1e-8). (3) Where brackets close, the full-W minimum is within 10 % of the certified R lower bound (the discarded tail at T = 160 is small on smooth minimizers), so Astra's ">10 % somewhere" prediction will be UNVERIFIED rather than confirmed at the accepted points. (4) Neighbouring log-slopes at accepted points (0.4→0.5, 0.5→0.6 if accepted) are compatible with a single slope within propagated errors, in the range 50–75 per unit L; no claim beyond the frozen range. (5) Controls: R160 ≤ R240 ≤ W on one fixed wave holds with certain nonnegative differences; nested-space monotonicity holds (λmin(192) ≤ λmin(160) at L = 0.7 within enclosures); a wave with a mismatched norm or flipped pole sign fails the scorer's control.
Kill: violated monotonicity or disjoint complete-score enclosures void the comparison until repaired. Cap: 2 single-core CPU hours total; stop and publish partial brackets.

## Scoring trial 2 (appended before running; trial 1 kept as scores_trial1_untruncated.log)
Trial 1: the certificates' 160-mode frozen minimizers carry genuine ~1e-40 components up to degree 318; D9's derivative tail bounds amplify them (n² per derivative, up to 12 derivatives) so no bracket closes even at L = 0.4 (relative width 2.2 at cutoff 512). Trial 2: freeze the SAME minimizers with |c| < 1e-16 zeroed (new frozen waves, explicitly recorded; their reduced scores change by < 1e-30), cutoffs 128 → 256 → 512 → 1024, stop at acceptance. PREDICTED: brackets close (≤ 10 %) at L = 0.40 and 0.50 both parities by cutoff ≤ 512; L = 0.60 by cutoff 1024 (even uncertain); L = 0.70 still fails.

## Scoring trial 3 (appended before running; trial 2 kept as scores_trial2_truncated.log)
Trial 2 result: truncation changed nothing; tails fall only like 1/T (L = 0.4 even: 2.3e-3, 4.6e-4, 2.2e-4, 1.1e-4 at cutoffs 128–1024). Diagnosis: the certificate minimizers do not vanish at ±L, so the zero-extended wave has a boundary jump and |F|² ~ t⁻²; the derivative tail method is then limited to order 1 and no cutoff reachable in the cap closes a 10 % bracket. This is the "positive discarded tail prevents the bracket" case Astra's protocol anticipated.
Trial 3 candidate rule (new, frozen before running): the upper bound on inf W may use ANY wave, so take the minimizer of the fixed-ruler reduced form (T = 160, 160 modes, double precision for selection only) restricted to the subspace of waves whose value and first four derivatives vanish at x = ±L (constraints are analytic Legendre endpoint formulas; no zero information). Freeze as exact decimals; score with the D9 scorer copy at cutoffs 128 → 256 → 512. PREDICTED: constrained candidates have tails ≤ 1e-9 by cutoff 512 (derivative order ≥ 5 available); at L = 0.40 and 0.50 the constrained W upper bound is within 10 % of the certified unconstrained lower bound (brackets close); at L = 0.60 the constrained minimum exceeds the certified bound by more than 10 % OR the tail still fails (bracket open); L = 0.70 open. Control: on one L = 0.5 wave certify R160 ≤ R240 ≤ W (cutoffs 160 and 240).
