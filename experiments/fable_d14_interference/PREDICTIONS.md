# D14 — "touch one prime and the whole pattern moves": is D13's rigidity the explicit-formula duality? (predictions before compute, 2026-09-11)
Idea: scaling the n = 2 weight by θ corresponds to replacing ζ(s) by ζ_θ(s) = ζ(s)·(1 − 2^{−s})^{(1−θ)·(stuff)} — a Beurling-type generalized prime system. The factor (1 − 2^{−s}) vanishes at s = 2πik/log 2, i.e. it injects "zeros" with Re s = 0 (off the critical line) at ordinates t_k = 2πk/log 2 = 9.065k. Weil positivity must see them: the negative direction of the perturbed form should carry spectral mass |F(t)|² concentrated near t₁ = 9.06 (and t₂ = 18.1).
Test A (floating, d12/d13 machinery): L = 0.60, both parities, θ = 0.9 on the n = 2 weight (form negative per D13). Take the most negative eigenvector f_θ, compute |F(t)|² on [0, 60], report the location of its largest peak and the fraction of ‖F‖² within ±1.5 of 9.065 and of 18.13. Control: the authentic minimizer (θ = 1) and a random vector. PREDICTED: peak of f_θ within ±1.0 of 9.065; fraction near 9.065 for f_θ at least 3× the control's.
Test B (compressibility, the tensor-network analogy): for L ∈ {0.4, 0.5, 0.6, 0.7}, the full-form minimizer's intrinsic dimension d(L) = number of leading Legendre modes needed to capture 1 − 1e-12 of its norm, versus the certificate's mode count N(L) = 1.2·T·L + 40. PREDICTED: d(L) ≤ 40 at every L and grows at most linearly, while N(L) grows with T ∝ e^{B_L}; i.e. the hard object is low-dimensional and the certificate cost is paying for the envelope, not for the object.
Kill: if Test A's peak is not near 9.06 in either parity, VOID the duality reading of D13.
