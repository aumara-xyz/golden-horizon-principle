# D11b — does the right-angle fact survive room enlargement? (predictions before compute, 2026-09-06)
Observed in D11 at L = 0.7 (N = 40 and 80, both parities): the pole coupling b is nearly orthogonal to the smallest-eigenvalue direction of C (|b·v_min|/‖b‖ = 3.9e-8 even, 6.7e-6 odd). Question: structural or coincidence?
Test (floating, numpy double, exploratory): for L ∈ {0.4, 0.5, 0.6, 0.7, 0.8}, both parities, T = max(1.3·T_env, 40), build R_T in the Legendre basis (d8 machinery), Householder on p, C = complement, v_min = eigenvector of λmin(C). Report ρ_L := |b·v_min|/‖b‖ and the share of q = bᵀC⁻¹b carried by v_min. Controls: (i) replace p by a random vector (expect ρ ~ 1/√N ≈ 0.1); (ii) arch-only model; (iii) N = 40 vs 80.
PREDICTED: ρ_L ≤ 1e-4 at every L in both parities (structural: the near-null direction of C is the smooth CCM-type ground state, which is nearly orthogonal to Hu because H nearly annihilates it and u is the pole direction); random control ρ ≥ 0.02. If ρ_L varies by more than 100× across L or exceeds 1e-3 anywhere, VOID the structural reading.
