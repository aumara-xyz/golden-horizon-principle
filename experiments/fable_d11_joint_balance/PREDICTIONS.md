# D11 predictions — can at most 8 Krylov steps certify the joint Schur balance? (Fable, written before compute, 2026-09-06)
Base: research branch tip 5c22b35 (D10 result fb55c90 is an ancestor; predictions 1a89261). Machine: this Mac (Darwin 25.1), /private/tmp/weil-arb-gTYWza/venv/bin/python, python-flint 0.6.0, mpmath 1.4.1. Budget: 30 min compute, 90 min total. Frozen: L = 7/10, T = 120, N = 80 per parity, orders m ∈ {2, 4, 8, 16, 32}, authentic κ = +2 (even) / −2 (odd). No zero ordinates, no φ, no new models, no enlarged L.

## What is already disclosed by D10 (not predictions)
σ_even ≈ 2.16779e-13 with min C ≈ 1.22601e-8; σ_odd ≈ 2.53198e-10 with min C ≈ 1.71888e-6; ‖C⁻¹b‖ ≈ 1.05 / 1.82; controls: arch-only and reversed-weight fail C > 0 (even) or σ > 0 (odd); even pole flip fails, odd pole flip stays positive; D9 full-W waves go negative under κ − 1e-4.

## Derivations done before reading Codex's code (recorded here, audited in PROOF.md)
Kernel: with q = e^r, K(r) = 2cosh(r/2) − e^{r/2}/(2 sinh r) = (q³ − q − 1)/(√q (q² − 1)); K(log 5/4) = −19/(18√5), K(2 log 5/4) = 5129/7380 — both reproduced by hand. The ½ comes from Re g(u) = ½[g(u) + g(−u)] turning the one-sided u-integral into a two-sided kernel. Pole kernel 2cosh((x−y)/2) from 2|C|² − 2|S|²; prime atoms −(w_n/2)[δ(y−x−log n) + δ(y−x+log n)]. Constant: ∫_0^∞(1 − e^{u/2})/sinh u du = ψ(¼) − ψ(½) = −π/2 − log 2, so the leftover scalar is a₀ − B with a₀ = ψ(¼) − log π. Schur: with Ue₀ = p/‖p‖ orthogonal, pᵀU(t,g) = ‖p‖t, and for C ≻ 0, fᵀR_κf = (g + tC⁻¹b)ᵀC(g + tC⁻¹b) + σt², σ = a + κ‖p‖² − bᵀC⁻¹b. Residual bracket: for any x, q − (2bᵀx − xᵀCx) = rᵀC⁻¹r ∈ [0, ‖r‖²/δ] when C ⪰ δI, r = b − Cx.

## P1 — which D10 step is most likely to fail an independent audit
PREDICTED: none of the numerical steps. The most fragile item is textual: the extension of the triangle sign obstruction to "almost-everywhere measurable phase/positive gauges" in KERNEL-PROOF.md is asserted by choosing a.e. triples without a written measure-theoretic argument; I expect it to be repairable, not wrong. Numerically I predict my independently built H (my d5 machinery, 192 bits) overlaps every entry of D10's authentic H (Opus-built, 256 bits) in both parities, with my radii ~1e-22 versus theirs ~1e-26, and that my independently certified δ agrees with D10's min C to 4 significant digits.

## P2 — does m ≤ 8 certify σ > 0 in BOTH parities at N = 80? PREDICTED: NO.
Reason: the residual bracket needs ‖r_m‖ ≤ √(σδ) ≈ 5e-11 (even) and ≈ 2e-8 (odd), i.e. 10 and 8 orders below ‖b‖, while unpreconditioned CG on a spread spectrum with min C/max C ~ 1e-9 (even) / 1e-7 (odd) gains far less than one order per step. The crude bracket pays the factor λ_max/δ on every unresolved component; a Gauss–Radau enclosure would pay the same factor and is not used (basic bracket only, as required).
## P3 — first successful order, or failure through 32
PREDICTED: failure through m = 32 in both parities (UNVERIFIED at every frozen order). Measured-limit predictions: ‖r_8‖/‖b‖ > 1e-2 and ‖r_32‖/‖b‖ > 1e-4 in the even sector; the lower bounds F_m increase monotonically with m; the bracket width ‖r_m‖²/δ exceeds 1e-3 at every m ≤ 32 (even).
Fallback diagnostic (labeled, not part of the short certificate): a full-size mpmath solve gives a reference q so the distance q − F_m can be reported; this uses a full inverse and is explicitly excluded from candidate construction.
## P4 — controls
Planted diagonal SPD (analytic q) and its rotation: bracket encloses q at every m, identical values (coordinate invariance). Indefinite C, singular C, interval-ambiguous δ: refused (no division). Arch-only and reversed-weight (D10's control matrices used as INPUTS, each certified or refused by MY certifier): even controls fail C > 0 (refused); odd controls have C > 0 with σ upper endpoint < 0 at some m ≤ 32 (negative finite witness (1, −x_m) scored directly), PREDICTED yes for odd arch-only, uncertain for odd reversed-weight. Even pole flip: C > 0 but σ upper endpoint < 0 at small m (PREDICTED m ≤ 4, witness produced). Odd pole flip: remains positive; the checker must not reject it. κ − 1e-4: negative finite witness by m ≤ 16 (even) and m ≤ 32 (odd); compared with, not conflated with, D9's full-W witnesses.
## P5 — authentic successes (if any) get doubled precision reruns and N = 40 replays; PREDICTED none occur.
## Success label if anything closes: "compressed response construction", because δ is certified by a full-size eigenbasis Gershgorin check (cost reported separately).
