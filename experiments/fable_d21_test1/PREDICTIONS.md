# D21 — Astra's Test 1: is the deletion failure in W or only in its lower approximation? (Fable, predictions before compute, 2026-09-13)
Executes experiments/astra_d20_review/PREDICTIONS.md §Test 1 at commit 07913fa. Frozen cases: ζ, L = 0.7, both parities; mutations: delete n = 4 only (weight log 2 → 0), delete n = 2 only, delete all visible arithmetic (prime-free). Proposal waves: the lowest eigenvector of the FIXED-RULER reduced form R_{L,T} (β = a(T) − B_authentic, T ∈ {160, 240}, 160 Legendre modes per parity; the reduced weights keep β a valid envelope because |Σ θ_n w_n cos| ≤ B_authentic), frozen as exact decimals of the double-precision coefficients. Exact scores via a provenance-recorded copy of D9's scorer (codex_d9_exact_scores/score.py, functions only; `compact` extended to also accumulate the compact prime integral so R_T can be evaluated on the same wave; the extension is disclosed in the code). Scores normalized by a rigorously enclosed norm. Classes: (a) certified W_mut-negative witness; (b) R-negative but W sign unresolved or positive on that wave; (c) neither resolved.
Astra's frozen prediction (theirs): the even deletion-of-4 claim will NOT produce a certified negative W witness; the prime-free odd witness survives.
Fable's predictions (mine, additional): (1) delete-4, even: the fixed-ruler reduced score on the proposal wave is itself POSITIVE at T = 160 and 240 (the D16/D17 −8e-13 was a moving-ruler artifact), and W_del4 on the wave is certified POSITIVE (class b with W positive) — reason: W ≥ 1.03e-13‖f‖² for every f, and deleting n = 4 changes W by +w₄·I₄(f) where I₄ is the shift-log4 correlation, positive on the D9 candidate; a negative I₄ of size > 1.5e-13 would be needed. (2) delete-4, odd: reduced score negative (D13/D16 both had −1e-4 range) and W_del4 certified NEGATIVE (class a) — the odd sector needs 4 (D13: −1.95e-5 with {2,3}); this is the one case where the two protocols could disagree with Astra's expectation, so I flag it as the informative one. (3) delete-2 and prime-free, both parities: class (a), certified W-negative, since the deleted terms are O(0.1–0.7) and the complete tails are far smaller. (4) identity W − R_T = nonnegative tail holds within enclosures on every wave; the tail is < 1e-6 relative on every proposal wave. (5) D9's saved frozen even wave rescored here overlaps D9's saved W interval (control).
Kill: any control failure (constant/linear shift overlaps, zero overlap for shift ≥ 2L, pole sign, D9 replay) voids the round; a wave whose W − R_T is certifiably negative means a scorer bug (VOID).
Budget: ≤ 30 min compute (background), 12 exact scorings + controls; survivor mutation (K = 80 nodes, 400 bits) on each class-(a) witness without changing the vector.
