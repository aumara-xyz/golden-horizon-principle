# D30 Task 1 (run by Fable on the Mac, zero cost) — complete certified visible-form floor table (predictions before compute, 2026-09-15)
Cases: ζ, both parities, L ∈ {0.4, 0.5, 0.6, 0.7}, T = 160, 160 modes per parity; plus L = 0.7 at T = 240, 192 modes. Visible prime set only (log n < 2L): L = 0.4 → {2}; 0.5 → {2}; 0.6 → {2, 3}; 0.7 → {2, 3, 4}. Machinery: d22_certify.py with the prime set filtered to visible shifts (this is the D24 §4.1 strengthening; at L = 0.7 nothing changes). Finite floor by the eigenbasis Gershgorin bound; three Schur tails with the visible B; the D24-repaired quadrature allowance (K + 1) is applied as a post-hoc subtraction 6·N·q_max from the finite floor, as in D24 §3.2, and the shifted checker used. Existing certified floors (D26: 0.4 odd 0.0141715765; D27: 0.4 even 0.000172308870, 0.5 odd 0.000180934197) are reproduced, not replaced.
PREDICTED: every new visible floor exceeds its D22 counterpart by 0–25 % where shifts were invisible (0.4, 0.5, 0.6) and equals it at 0.7; log-slopes between rooms increase with L in both parities; the e^{2L} model (D23 §A) fits the certified table with slope error ≤ 10 % in both parities and the linear model fails; at L = 0.7 the T = 240 floor exceeds the T = 160 floor by 20–30 %.
Kill: a visible floor BELOW its D22 counterpart is impossible for a correct strengthening → VOID and debug.
