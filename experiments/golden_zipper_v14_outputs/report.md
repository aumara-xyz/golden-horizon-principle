# Golden Zipper v14 - Observer Afterglow Hysteresis

Toy telemetry only. Not physics evidence. Not proof of GHP.

Best model by mean composite: `click_only` with `0.492`

Key comparisons:
- `click_only` mean composite: `0.492`
- `click_afterglow` mean composite: `0.335`
- `click_afterglow_refresh` mean composite: `0.334`
- `click_afterglow_refresh` mean surrogate gap: `-0.061`

Interpretation:
- If `click_afterglow` beats `click_only`, the observer seems to benefit from a short witness trace.
- If `click_afterglow_refresh` beats both, then memory behaves more like click-plus-refresh than either pure click or pure accumulation.
