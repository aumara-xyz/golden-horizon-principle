# D23 — two shape questions on the certified data (predictions before compute, 2026-09-13)
A. Profile model on the D22 certified lower bounds (T = 160, 160 modes; four points per parity, arithmetic only, no new certificates). Compare two 2-parameter models for y = log λ: (M1) y = A − B·L; (M2) y = A − C·e^{2L}. Discriminator: relative RMS residual of the fitted slopes over the three intervals. PREDICTED: M2 fits both parities with slope errors ≤ 10 %, M1 fails (≥ 20 %); the fitted C is of order 1–2. This is a model comparison on four points; no asymptotic law is claimed. Kill: if neither model reaches 15 %, report "no two-parameter law on four points".
B. Prolate hypothesis. For each certified minimizer (unconstrained, from d22_cert_*_T160_N160.json, frozen 40-digit coefficients), compute the overlap |⟨c, ψ_k(Ω)⟩| with the leading even (k = 0) or odd (k = 1) prolate function of bandwidth Ω on [−L, L], obtained as the top eigenvector of the time-frequency limiting operator in the same Legendre basis (P(Ω)_mn = 2∫₀^Ω F_m F_n dt, floating). Sweep Ω ∈ [2, 80]. PREDICTED: max overlap ≥ 0.99 in every case; the best Ω*(L) increases with L and Ω*·L (the prolate parameter c) lies between 3 and 12; control: overlap with the SECOND prolate of the same parity is ≤ 0.3 at Ω*. Kill: max overlap < 0.9 anywhere → the minimizer is not a single prolate and the hypothesis is VOID as stated.
