# Preregistration — ZETA-CUBE-NULL v1 (the cube test, run properly: controls first)

- test_id: ZETA-CUBE-NULL-v1
- date_locked: **2026-08-01 (SIGNED)** — owner chat directive ("i approve everything that is waiting, please just push all this now"), lane countersigned. Lane: **Riemann adjacency, analogue only** (Addendum B.4 fence applies in full: nothing here may appear in receipts, laws, gates, or product copy as support).
- runtime: Python 3, laptop. Data: Odlyzko's table of the first 100,000 zeta zero ordinates (public), first 10,000 used; integrity check = first three ordinates match mpmath to 6 decimals.

## 1. The declared prediction (stated before the run)

Known mathematics (equidistribution of zero ordinates) predicts the base-3 digit mapping is
structure-blind: **the controls will pass — the expected verdict is NULL.** A null here is a
real result: it closes this door with a receipt instead of a vibe. The only surprising outcome
would be the real zeros separating from ALL controls, which would be an equidistribution
anomaly (about digit uniformity, never about RH), to be escalated as its own question.

## 2. Protocol (controls FIRST, by law)

- Mapping (fixed): ordinate t → cell (floor(3t) mod 3, floor(9t) mod 3, floor(27t) mod 3); 27-cell occupancy over the first 10,000 ordinates.
- Structure scores (both fixed now): S1 = chi-square of occupancy vs uniform (df 26); S2 = mutual information between consecutive cell indices (order structure).
- Controls, run BEFORE the real zeros: (a) 10,000 uniform random reals on the same range (RNG seed 6000); (b) the zeros with their GAPS SHUFFLED (seed 6001 — destroys order, keeps the gap multiset); (c) the first 10,000 primes scaled to the same range.
- **Kill condition (= the predicted outcome):** if the real zeros' scores fall within the controls' spread (2.5–97.5 percentile of 200 control replicates for S1; same for S2), the mapping carries no information → verdict **NULL**, reported as prominently as any hit.

## 3. No-upgrade sentences

A NULL confirms known equidistribution and closes the digit-cube door with a receipt. A deviation would be a numerics/equidistribution finding only. Under no outcome does this test bear on the Riemann Hypothesis, GHP, φ, or the 27-cell frame's symbolic uses.

**Signed:** Peter Viviani (owner), by chat directive 2026-08-01.
