# Turok / Bateman Projection Bridge Watchlist

**Date:** 2026-06-27  
**Status:** watchlist / source audit needed  
**GHP placement:** research only; not core paper; not proof update

## Why This Is Being Tracked

The candidate bridge is interesting because it appears to ask a GHP-shaped question:

```text
larger hidden / nonstandard state space
-> projection, trace, or modified probability rule
-> positive observer-readable public probabilities
```

That resembles the GHP finite-access target:

\[
E_t:M_t\to N_t
\]

where \(M_t\) is a larger hidden / ambient process and \(N_t\) is the finite observer-readable public record algebra.

## Current Public Source Status

Initial public search found podcast / announcement-level material around Neil Turok and Sam Bateman discussing quadratic gravity, negative-norm states, Krein-space-like structures, and a modified Born-rule idea. A primary paper with the exact projection / trace formula was not found during this first pass.

Until a primary source is located, this stays **watchlist only**.

## What Would Make It Useful

The bridge becomes mathematically useful only if a primary source provides an exact rule such as:

- the hidden state space \(K\),
- the indefinite metric \(\eta\),
- allowed dynamics \(U\),
- initial / final / observable projectors,
- a probability rule \(p(a)\) or \(p(a\mid i,f)\),
- and a proof or argument for positivity / normalization.

The most relevant GHP question is:

```text
Can projection or trace discipline render a larger hidden space
into stable, positive, finite public records without leaking the
hidden sector?
```

## Safe Mapping

| Candidate object | GHP analogue | Status |
|---|---|---|
| indefinite hidden state space | hidden / ambient process \(M_t\) | possible analogy |
| projection / trace / modified Born rule | finite-access map \(E_t:M_t\to N_t\) | source-dependent |
| positive public probabilities | observer-readable public record | possible bridge |
| negative-norm / ghost sector | hidden residue or gauge-like redundancy | do not mythologize |
| final boundary condition | two-boundary / post-selection-like constraint | source-dependent |

## Hard Non-Claims

Do not claim:

- Turok / Bateman proves GHP.
- Quadratic gravity validates Fibonacci selection.
- Negative-norm states are hidden consciousness.
- Ghosts are dark matter, dark energy, observers, or souls.
- A modified Born rule proves observer-boundary physics.
- Projection operators solve the GHP write law.
- Krein spaces derive the Fibonacci Markov kernel.
- This closes the Dynamics Gap, Matter Embedding Gap, VPH, or cosmology.

## Next Test To Run

Run **TBP-001 — Indefinite Projection To Positive Public Record Probe** only as a generic bridge-shape toy unless / until the primary formula appears.

Test shape:

```text
hidden indefinite metric space eta = diag(1, 1, -1)
-> candidate projection / trace rules
-> public probabilities
-> positivity, normalization, stability, leakage checks
```

Success would mean:

- some projection rules turn indefinite hidden data into sane public probabilities,
- hidden negative-sector details remain unreconstructable,
- the public map resembles finite-access projection more than arbitrary renormalization.

Failure would mean:

- positivity or normalization fails,
- public probabilities leak hidden-sector labels,
- the result is just ad hoc clipping / renormalization,
- or it has no structural similarity to \(E:M\to N\).

## Current Recommendation

Hopeful as a **finite-access projection analogue**.

Not ready for core paper.

Best next action:

1. Locate primary source / exact formulas.
2. Run TBP-001 generic toy.
3. If formulas appear, rerun TBP-001 with the real rule.
4. Only then decide between reject / watchlist / research ledger / master addendum.
